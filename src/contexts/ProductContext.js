//Step 2. I created the contexts directory and
// now and creating the ProductContext.js file
//  to import createContext and create our productContext

import { createContext } from "react";

export const ProductContext = createContext();